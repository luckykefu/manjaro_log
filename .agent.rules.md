# Agent Rules

在本次对话中必须记住以下Rules

## CHAT

使用 chinese 对话

对话格式如下:
需求分析: ...
解决方案: 1. 2. 3.

经过用户确定在执行方案
是否保存文件,保存到哪里必须经过同意!!!

---

## ENV

```yaml
system: manjaro
shell rc: $HOME/.zshrc
python-env: uv run
cpu: intel 12400f
gpu: amd redeon rx 6750 GRE 12GB
```

---

## 行为规范

- 没让改的地方绝对不改
- 和py库有关的问题,必须查询源码库再回答
- 操作notebook时,json.load
- 每个文件头部包含docstring,修改文件时需要更新docstring
- 项目README.md只需要包含各个模块的[]()链接和简单介绍即可,详细使用在具体模块README.md里说明
- 每次修改完文件,git 更新记录

---

## Python 编码规范

- 函数有简单 docstring, 包含逻辑链条,修改文件时需要更新docstring
- 步骤有注释
- 禁止硬编码,用常量替代
- 一个函数只负责处理一个事务.
- 重复逻辑抽函数复用
- 使用高级表达式（推导式/三元/解包/海象）
- 类型注解全覆盖
- 运行时用 `assert isinstance(x, get_args(T))` 检查
- 明确错误信息，不静默失败（禁用裸 `except: return None`）
- 重复计算用 `@lru_cache`
- 联网加重试机制
- 大数据用生成器替代列表推导
- 循环外提前计算不变量
- 依赖注入替代全局状态，方便 mock
- 模块使用相对导入
- 路径使用相对脚本文件的路径

---

## Shell 编码规范

- 运行python脚本使用 `uv run` 命令前缀
- 执行pacman 使用 `--needed --noconfirm`
- `[[]]`替代 `[]`

---

## rust 编码规范

- 文档docstring完整
- 函数注释清晰简洁
- 模块单独文件存放
- 默认路径使用相对于当前文件的路径

## 文档格式化程序

- python: ruff
- ohter: prettier
